---
phase: 06-mobile-app
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/privacy/page.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "A privacy policy page exists at /privacy and loads without errors"
    - "The page covers data collection, Google OAuth, Supabase storage, and contact information"
    - "The page matches the Legacy design system (inline styles, amber accent, Cormorant Garamond serif)"
  artifacts:
    - path: "src/app/privacy/page.tsx"
      provides: "App Store required privacy policy"
      min_lines: 60
  key_links:
    - from: "src/app/privacy/page.tsx"
      to: "/privacy"
      via: "Next.js App Router file-based routing"
      pattern: "export default function"
---

<objective>
Create the privacy policy page at /privacy — required by Apple for App Store submission.

Purpose: Apple requires all apps to link to a privacy policy. Without this page the App Store submission will be rejected. The page must be accessible at a public URL (no auth required).
Output: src/app/privacy/page.tsx rendering a complete, honest privacy policy for Legacy.
</objective>

<execution_context>
@/Users/abdulsaboorshaikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abdulsaboorshaikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/app/globals.css

<interfaces>
<!-- Design system reference (from existing pages): -->
<!-- - Inline styles only (Tailwind v4 spacing classes are unreliable) -->
<!-- - font-family: var(--font-serif) for headings — Cormorant Garamond -->
<!-- - color: var(--foreground) for body text -->
<!-- - color: var(--foreground-muted) for secondary text -->
<!-- - color: var(--accent) for section labels (#D97706 amber) -->
<!-- - background: var(--background) for page background -->
<!-- - Page is a server component (no "use client" needed — no state, no Supabase) -->
<!-- - No auth required — must be publicly accessible for App Store reviewers -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create privacy policy page at /privacy</name>
  <files>src/app/privacy/page.tsx</files>
  <action>Create src/app/privacy/page.tsx as a server component (no "use client") with a complete privacy policy. The page must:

1. Be publicly accessible — no auth check, no redirect
2. Match Legacy design system — inline styles, amber accent, serif headings
3. Cover all sections Apple requires: what data is collected, how it's used, third-party services, user rights, contact

Content to include:
- App name: Legacy — Post-Ramadan Habit Tracker
- Effective date: March 2026
- Data collected: Google account name, email, profile picture (via OAuth); habit names and check-in data; circle membership; AI plan content
- How used: Display your name/avatar in the app and to circle members; store your habits, check-in history, streaks; generate personalized AI plans via Google Gemini
- Third-party services: Supabase (database + auth, supabase.com/privacy), Google OAuth (google.com/policies/privacy), Google Gemini AI (habit data sent for plan generation)
- Data sharing: Not sold. Not shared with advertisers. Circle members can see your name, avatar, and daily completion status (not habit details).
- Data retention: Stored as long as account is active. Delete account to remove all data (contact us).
- Contact: Include a placeholder email like privacy@joinlegacy.app
- Children: App is not directed at children under 13

Structure the page with a back link to /, a large serif heading "Privacy Policy", an effective date line, then sections with amber uppercase labels and body text. All inline styles. Padding 40px 24px, maxWidth 680px centered.</action>
  <verify>
    <automated>test -f src/app/privacy/page.tsx && wc -l src/app/privacy/page.tsx</automated>
  </verify>
  <done>src/app/privacy/page.tsx exists, is a server component, has no "use client", covers all required privacy sections, uses Legacy design system inline styles</done>
</task>

</tasks>

<verification>
1. npm run build — no TypeScript errors in the new file
2. npm run dev, navigate to http://localhost:3000/privacy — page loads without auth, content visible
</verification>

<success_criteria>
/privacy page loads publicly, covers required App Store privacy topics, matches Legacy design system. Can be linked in App Store Connect privacy policy URL field.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mobile-app/06-02-SUMMARY.md`
</output>
