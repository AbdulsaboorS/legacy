---
phase: 06-mobile-app
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - codemagic.yaml
autonomous: true
requirements:
  - MOB-01
  - MOB-05

must_haves:
  truths:
    - "codemagic.yaml exists at project root with a valid ios-workflow"
    - "Workflow installs dependencies, syncs Capacitor, signs, and builds an IPA"
    - "Publishing config targets TestFlight (not App Store direct)"
  artifacts:
    - path: "codemagic.yaml"
      provides: "CI/CD pipeline for iOS build and TestFlight upload"
      contains: "ios-workflow"
  key_links:
    - from: "codemagic.yaml"
      to: "App Store Connect"
      via: "Codemagic app_store_connect publishing integration"
      pattern: "submit_to_testflight: true"
---

<objective>
Create codemagic.yaml defining the iOS build pipeline that produces a signed IPA and uploads it to TestFlight.

Purpose: Codemagic is the CI/CD layer between code changes and App Store submissions. The human will still need to configure signing certificates and App Store Connect API keys in the Codemagic UI, but the YAML pipeline is ready to run. This is Wave 1 (independent — no dependency on other plans).
Output: codemagic.yaml at project root, ready to be pushed and connected to Codemagic.
</objective>

<execution_context>
@/Users/abdulsaboorshaikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abdulsaboorshaikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-mobile-app/06-RESEARCH.md

<interfaces>
<!-- From research Pattern 4 — Codemagic YAML structure -->
<!-- Bundle ID: app.joinlegacy -->
<!-- Xcode workspace path: ios/App/App.xcworkspace (generated by npx cap add ios) -->
<!-- Xcode scheme: App (default Capacitor scheme) -->
<!-- Node version: 20.14.0 (match project) -->
<!-- submit_to_testflight: true, submit_to_app_store: false (for initial builds) -->
<!-- APP_STORE_APPLE_ID: placeholder — human fills in after creating App Store listing -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create codemagic.yaml</name>
  <files>codemagic.yaml</files>
  <action>Create codemagic.yaml at the project root. Use the structure from research Pattern 4 exactly, with these specifics:

```yaml
workflows:
  ios-workflow:
    name: Legacy iOS
    max_build_duration: 120
    instance_type: mac_mini_m1
    integrations:
      app_store_connect: codemagic
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: app.joinlegacy
      vars:
        APP_STORE_APPLE_ID: Var.APP_STORE_APPLE_ID  # Set in Codemagic Team > Environment Variables
        XCODE_WORKSPACE: "ios/App/App.xcworkspace"
        XCODE_SCHEME: "App"
      node: v20.14.0
      xcode: latest
    scripts:
      - name: Install npm dependencies
        script: npm ci
      - name: Install CocoaPods
        script: |
          cd ios/App && pod install
      - name: Sync Capacitor
        script: npx cap sync ios
      - name: Set up code signing
        script: xcode-project use-profiles
      - name: Increment build number
        script: |
          LATEST=$(app-store-connect get-latest-app-store-build-number "$APP_STORE_APPLE_ID" 2>/dev/null || echo "0")
          agvtool new-version -all $(($LATEST + 1))
      - name: Build IPA
        script: |
          xcode-project build-ipa \
            --workspace "$XCODE_WORKSPACE" \
            --scheme "$XCODE_SCHEME"
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false
```

Add a comment block at the top explaining what human setup is required:
- Codemagic Team > Integrations: Connect App Store Connect API key (issuer ID + key ID + .p8 file)
- Codemagic Team > Code signing: Upload distribution certificate (.p12) and provisioning profile
- Codemagic Team > Environment variables: Set APP_STORE_APPLE_ID to the numeric App Store Connect app ID
- ios/ directory must be generated first via `npx cap add ios` before this pipeline can run</action>
  <verify>
    <automated>test -f codemagic.yaml && grep -c "ios-workflow\|submit_to_testflight\|app.joinlegacy" codemagic.yaml</automated>
  </verify>
  <done>codemagic.yaml exists, contains ios-workflow, references app.joinlegacy bundle ID, targets TestFlight, has human setup comment block</done>
</task>

</tasks>

<verification>
1. cat codemagic.yaml — visually confirm structure matches expected workflow
2. yamllint codemagic.yaml (or python3 -c "import yaml; yaml.safe_load(open('codemagic.yaml'))") — valid YAML
</verification>

<success_criteria>
codemagic.yaml exists and is valid YAML. Human can connect repo to Codemagic, upload signing assets, and trigger a build without editing the YAML.
</success_criteria>

<output>
After completion, create `.planning/phases/06-mobile-app/06-03-SUMMARY.md`
</output>
