---
phase: 05-web-app-flow-polish
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/dashboard/DashboardClient.tsx
autonomous: true
requirements:
  - JOIN-01
must_haves:
  truths:
    - "No user-facing 'AI' label appears on habit cards in the dashboard"
    - "Empty state generate button reads 'Want a personalized plan?'"
    - "Plan toggle label reads 'Your Plan'"
    - "Regenerate button reads 'Refresh plan'"
    - "Refine button reads 'Refine your plan'"
    - "Refine textarea placeholder reads 'Tell me how to adjust it...'"
  artifacts:
    - path: "src/app/dashboard/DashboardClient.tsx"
      provides: "Updated AI copy strings"
  key_links:
    - from: "DashboardClient.tsx"
      to: "user-facing strings"
      via: "literal string replacements at known line numbers"
      pattern: "Want a personalized plan"
---

<objective>
Replace all user-facing "AI" labels in DashboardClient.tsx with personalized, human-feeling copy per the locked decisions from CONTEXT.md.

Purpose: Remove clinical "AI" terminology from the user interface. No route renames, no variable renames, no logic changes — pure string replacements.
Output: DashboardClient.tsx with 5 updated string literals.
</objective>

<execution_context>
@/Users/abdulsaboorshaikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abdulsaboorshaikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/dashboard/DashboardClient.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace AI copy strings in DashboardClient.tsx</name>
  <files>src/app/dashboard/DashboardClient.tsx</files>
  <action>
Make exactly these 5 string replacements in DashboardClient.tsx. Do NOT change variable names, function names, API route strings, code comments, or any other content.

**Replacements (confirmed from source audit):**

1. Line ~524 — generate button text:
   - Find: `✨ Generate AI Plan`
   - Replace: `✨ Want a personalized plan?`

2. Line ~537 — plan toggle label:
   - Find: `✨ AI Masterplan`
   - Replace: `✨ Your Plan`

3. Line ~628 — refine button label:
   - Find: `✏️ Refine Plan`
   - Replace: `✏️ Refine your plan`

4. Line ~635 — regenerate button label:
   - Find: `🔄 Regenerate`
   - Replace: `🔄 Refresh plan`

5. Line ~664 — refine textarea placeholder:
   - Find: `e.g. make it less intense, add more Sunnah context...`
   - Replace: `Tell me how to adjust it...`

**Do NOT change:**
- `This will replace your current plan. Continue?` — already matches spec, leave as-is
- Any variable named `generatingHabitId`, `showRegenerateConfirm`, `aiPlan`, etc.
- Any API route strings (`/api/ai/plan/generate`, etc.)
- Any code comments

Use the Read tool first to find the exact current strings before editing — line numbers are approximate.
  </action>
  <verify>
    <automated>grep -n "Generate AI Plan\|AI Masterplan\|Refine Plan\|🔄 Regenerate\|make it less intense" /Users/abdulsaboorshaikh/legacy/src/app/dashboard/DashboardClient.tsx | wc -l</automated>
  </verify>
  <done>grep returns 0 matches for all 5 old strings. New strings are present. npm run build passes.</done>
</task>

</tasks>

<verification>
Run: `grep -n "Generate AI Plan\|AI Masterplan\|Refine Plan\|🔄 Regenerate\|make it less intense" src/app/dashboard/DashboardClient.tsx` — should return zero results.
Run: `grep -n "personalized plan\|Your Plan\|Refine your plan\|Refresh plan\|Tell me how" src/app/dashboard/DashboardClient.tsx` — should return 5 matches.
Run: `npm run build` — passes.
</verification>

<success_criteria>
- All 5 old AI-labelled strings replaced with personalized copy
- Zero variable/function/route renames
- npm run build passes with zero new errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-app-flow-polish/05-03-SUMMARY.md`
</output>
