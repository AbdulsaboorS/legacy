---
phase: 05-web-app-flow-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260318_phase5_description.sql
  - src/lib/types.ts
autonomous: true
requirements:
  - JOIN-01
  - JOIN-02
must_haves:
  truths:
    - "halaqas table has a description TEXT column accepting up to 150 chars"
    - "Halaqa TypeScript interface includes description?: string | null"
    - "Owner UPDATE RLS policy on halaqas is active"
  artifacts:
    - path: "supabase/migrations/20260318_phase5_description.sql"
      provides: "ADD COLUMN migration + conditional owner UPDATE RLS policy"
      contains: "ALTER TABLE halaqas"
    - path: "src/lib/types.ts"
      provides: "Halaqa interface with description field"
      exports: ["Halaqa"]
  key_links:
    - from: "supabase/migrations/20260318_phase5_description.sql"
      to: "halaqas table"
      via: "ALTER TABLE ... ADD COLUMN IF NOT EXISTS description"
      pattern: "ADD COLUMN IF NOT EXISTS description"
    - from: "src/lib/types.ts"
      to: "HalaqaClient.tsx, CircleDetailClient.tsx, JoinClient.tsx"
      via: "Halaqa interface description field"
      pattern: "description\\?.*string.*null"
---

<objective>
Add the optional `description` column to the `halaqas` table and update the `Halaqa` TypeScript interface so all downstream UI work can build against a stable contract.

Purpose: This is the foundation that unblocks the circle description UI surfaces in Plan 05-04. It also adds the RLS UPDATE policy needed for owner description edits.
Output: SQL migration file and updated types.ts.
</objective>

<execution_context>
@/Users/abdulsaboorshaikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abdulsaboorshaikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Supabase migration for halaqas.description</name>
  <files>supabase/migrations/20260318_phase5_description.sql</files>
  <action>
Create the migration file with these exact contents (wrapped in BEGIN/COMMIT):

1. ADD COLUMN IF NOT EXISTS: `description TEXT` with CHECK constraint `(description IS NULL OR char_length(description) <= 150)` on the `halaqas` table.

2. Conditional RLS UPDATE policy: Use a DO $$ BEGIN ... IF NOT EXISTS (SELECT 1 FROM pg_policies WHERE tablename = 'halaqas' AND policyname = 'Owner can update halaqa') THEN CREATE POLICY "Owner can update halaqa" ON halaqas FOR UPDATE USING (created_by = auth.uid()); END IF; END$$; block so the migration is idempotent — if the policy already exists it skips silently.

After writing the file, apply it to the Supabase project using the Supabase MCP tool (project ref: czlfzfuqilvdpdpqnaso). If MCP is unavailable, provide the SQL for manual application in the Supabase dashboard SQL editor and note this as a manual step.

Do NOT use `supabase db push` — the project uses direct MCP or dashboard migration, not the CLI.
  </action>
  <verify>
    <automated>
      If applied via MCP: confirm no error response.
      If manual: SQL file exists at supabase/migrations/20260318_phase5_description.sql with correct ADD COLUMN and DO $$ block.
    </automated>
  </verify>
  <done>Migration file committed. halaqas table has description column (or file ready for manual apply). Owner UPDATE RLS policy created or confirmed to exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add description field to Halaqa TypeScript interface</name>
  <files>src/lib/types.ts</files>
  <action>
In the `Halaqa` interface (currently lines 64-73), add one field after `created_at`:

```typescript
description?: string | null;   // NEW — Phase 5, max 150 chars
```

The full updated Halaqa interface should be:
```typescript
export interface Halaqa {
  id: string;
  name: string;
  created_by: string | null;
  invite_code: string;
  gender: "Brother" | "Sister";
  is_public: boolean;
  max_members: number;
  created_at: string;
  description?: string | null;   // NEW — Phase 5, max 150 chars
}
```

No other changes to types.ts. Do not touch PRESET_HABITS, PROPHETIC_QUOTES, HabitPlan, FeedRow, or any other interfaces.
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>types.ts updated, TypeScript build passes with no new errors.</done>
</task>

</tasks>

<verification>
Run `npm run build` — zero TypeScript errors. The Halaqa interface in types.ts exports `description?: string | null`. Migration SQL file exists and has been applied (or is ready for manual apply).
</verification>

<success_criteria>
- `supabase/migrations/20260318_phase5_description.sql` exists with ADD COLUMN + conditional RLS policy
- `src/lib/types.ts` Halaqa interface has `description?: string | null`
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-web-app-flow-polish/05-01-SUMMARY.md`
</output>
