---
phase: 03-ai-plan-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/ai/plan/generate-stream/route.ts
  - src/app/api/ai/plan/refine/route.ts
autonomous: true
requirements:
  - AI-02
  - AI-03
  - AI-04

must_haves:
  truths:
    - "POST /api/ai/plan/generate-stream streams SSE chunks from Gemini in real time"
    - "Unauthenticated requests to generate-stream return 401 before any Gemini call"
    - "Refine prompt no longer includes dailyActions in the expected JSON structure"
    - "Refine route still streams correctly after the prompt fix"
  artifacts:
    - path: "src/app/api/ai/plan/generate-stream/route.ts"
      provides: "Streaming plan generation endpoint (Edge runtime, SSE)"
      exports: ["POST"]
    - path: "src/app/api/ai/plan/refine/route.ts"
      provides: "Streaming plan refinement with fixed prompt"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/ai/plan/generate-stream/route.ts"
      to: "Gemini generateContentStream"
      via: "model.generateContentStream(prompt)"
      pattern: "generateContentStream"
    - from: "src/app/api/ai/plan/generate-stream/route.ts"
      to: "SSE output"
      via: "ReadableStream + data: JSON\\n\\n format"
      pattern: "data:.*\\\\n\\\\n"
---

<objective>
Create a new streaming plan generation endpoint and fix the refine prompt.

Purpose: The old generate-and-save route is synchronous — it times out before Gemini responds on slow connections. Replacing it with a streaming SSE endpoint lets the dashboard display plan text in real time and call /api/ai/plan/save once the stream completes. The refine route has a stale prompt that references dailyActions which no longer exist in the plan structure.

Output:
- src/app/api/ai/plan/generate-stream/route.ts (new streaming generation route)
- src/app/api/ai/plan/refine/route.ts (prompt fix: remove dailyActions)
</objective>

<execution_context>
@/Users/abdulsaboorshaikh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abdulsaboorshaikh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-plan-generation/03-CONTEXT.md

<interfaces>
<!-- Key types the executor needs. No codebase exploration required. -->

From src/lib/types.ts:
```typescript
export interface WeekEntry { week: number; focus: string; target: string; }
export interface ActionableStep { step: string; description: string; }
export interface HabitPlan {
  id: string; habit_id: string; user_id: string; is_active: boolean;
  version: number; core_philosophy: string | null;
  actionable_steps: ActionableStep[] | null;
  weekly_roadmap: WeekEntry[] | null;
  created_at: string;
}
```

Existing refine route (reference for Edge runtime + SSE pattern):
```typescript
// src/app/api/ai/plan/refine/route.ts
export const runtime = "edge";
const supabase = createServerClient(NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, {
  cookies: { getAll() { return request.cookies.getAll(); }, setAll() {} }
});
// SSE pattern: data: ${JSON.stringify({ text })}\n\n
// Terminator: data: [DONE]\n\n
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create streaming generate-stream route</name>
  <files>src/app/api/ai/plan/generate-stream/route.ts</files>
  <action>
Create a new file at src/app/api/ai/plan/generate-stream/route.ts. This is a streaming SSE endpoint that generates a habit plan in real time without saving — the dashboard accumulates the stream then calls /api/ai/plan/save.

Requirements:
- export const runtime = "edge"
- Auth guard using createServerClient from @supabase/ssr with request.cookies.getAll() pattern (same as refine route). Return 401 before any Gemini call if unauthenticated.
- Accept POST body: { habitId: string, habitName: string, gender?: string }
- Return 400 if habitId or habitName is missing
- Use GoogleGenerativeAI with model "gemini-2.0-flash" (NO responseMimeType — breaks streaming per project decision)
- Prompt: same structure as the existing generate-and-save route but without JSON enforcement via config. Use prompt-only enforcement. Request this exact JSON structure:
  {
    "corePhilosophy": "2-3 sentences...",
    "actionableSteps": [{ "step": "...", "description": "..." }, ...3 items],
    "weeklyRoadmap": [{ "week": 1, "focus": "...", "target": "..." }, ...4 weeks]
  }
  DO NOT include dailyActions in the prompt or expected output.
- Stream via ReadableStream + generateContentStream. Each chunk: data: ${JSON.stringify({ text })}\n\n
- Terminate with: data: [DONE]\n\n
- On stream error: emit data: ${JSON.stringify({ error: "Stream failed" })}\n\n then close
- Response headers: Content-Type: text/event-stream, Cache-Control: no-cache, Connection: keep-alive
  </action>
  <verify>
    <automated>curl -s -X POST http://localhost:3000/api/ai/plan/generate-stream -H "Content-Type: application/json" -d '{"habitId":"test","habitName":"test"}' | head -3</automated>
  </verify>
  <done>Route file exists. Unauthenticated requests return 401. Authenticated requests begin streaming SSE chunks. No dailyActions anywhere in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Fix refine prompt — remove dailyActions</name>
  <files>src/app/api/ai/plan/refine/route.ts</files>
  <action>
Open src/app/api/ai/plan/refine/route.ts. The prompt currently includes "dailyActions" in the expected JSON structure and instructs Gemini to return all 28 daily entries. This is wrong — dailyActions was intentionally removed from the plan structure.

Specific changes:
1. In the prompt string, remove the dailyActions line from the expected JSON structure:
   REMOVE: `"dailyActions": [ ...all 28 entries with day, action, tip... ]`
2. Remove the instruction: "All 28 dailyActions entries are required. Do not stop before day 28. Keep each action under 80 characters. Keep each tip under 100 characters."
3. The revised prompt should request ONLY: corePhilosophy, actionableSteps (3 items), weeklyRoadmap (4 weeks) — matching the HabitPlan type exactly.
4. Keep the prompt instruction: "Return ONLY valid JSON. No markdown. No code blocks. No extra text."
5. Do not change anything else in the file — auth guard, streaming logic, response headers all stay the same.
  </action>
  <verify>
    <automated>grep -n "dailyActions" /Users/abdulsaboorshaikh/legacy/src/app/api/ai/plan/refine/route.ts</automated>
  </verify>
  <done>grep returns no matches — dailyActions is fully removed from the refine route. The expected JSON structure in the prompt matches the HabitPlan type: corePhilosophy + actionableSteps + weeklyRoadmap only.</done>
</task>

</tasks>

<verification>
1. No dailyActions reference in refine route: `grep -n "dailyActions" src/app/api/ai/plan/refine/route.ts` returns empty
2. generate-stream route exists: `ls src/app/api/ai/plan/generate-stream/route.ts`
3. generate-stream uses edge runtime: `grep "runtime" src/app/api/ai/plan/generate-stream/route.ts` returns "edge"
4. generate-stream has no dailyActions: `grep "dailyActions" src/app/api/ai/plan/generate-stream/route.ts` returns empty
5. TypeScript compiles: `npm run build` passes (or `npx tsc --noEmit`)
</verification>

<success_criteria>
- /api/ai/plan/generate-stream exists as an Edge SSE route
- Unauthenticated requests to generate-stream return 401 (no Gemini call made)
- Refine prompt requests only corePhilosophy + actionableSteps + weeklyRoadmap (no dailyActions)
- Both files pass TypeScript compilation with no errors
</success_criteria>

<output>
After completion, create .planning/phases/03-ai-plan-generation/03-01-SUMMARY.md
</output>
